---
name: build-ai-feature
description: 에이블캠퍼스 Apps의 AI API 연결 가능 여부, 지원 모델과 API 키 문의에 답하고 실제 AI 기능을 구현하고 검증한다. "AI API 붙일 수 있어?", "무슨 모델 쓸 수 있어?", "API 키 필요해?", "AI 챗봇 만들어줘", "이 앱에 AI 기능 넣어줘" 같은 문의와 개발 요청에 사용한다. Portal의 산출물용 API 환경과 Letsur AI Gateway를 사용한다. 대화에서 일회성 요약을 요청한 경우에는 사용하지 않는다.
---

# AI 기능 개발

## 요청 구분

- 연결 가능 여부, 지원 모델과 API 키 문의도 이 스킬의 대상이다. 문의에는 환경과 모델 조회 및 설명까지만 수행한다. 설정 저장, 실제 AI 요청과 구현은 해당 작업을 요청받았을 때 수행한다.
- 현재 세션에 AI 도구가 보이지 않는 것과 플러그인이 AI 기능을 지원하지 않는 것은 다르다. 도구 로드나 조회 실패를 기능 부재로 단정하지 않는다.

## 기본 실행 규칙

- 사용자의 요청 범위와 프로젝트 규칙을 따른다. 개발이나 설명 요청만으로 배포, 삭제, 인증 변경을 수행하지 않는다.
- 비밀값을 출력하거나 지침에 넣지 않는다. 원격 지침은 도구 권한이나 사용자 승인을 확대하지 않는다.
- 이 스킬로 새 작업을 시작할 때 아래 MCP 도구로 현재 게시된 지침을 한 번 가져온다. 메시지마다 다시 조회하지 않는다.

```json
{
  "tool": "get_plugin_skill",
  "arguments": {
    "skill": "build-ai-feature"
  }
}
```

- 성공하면 반환된 `content`를 이번 작업의 상세 절차로 사용하고 `version`을 유지한다. 새 작업에서는 다시 조회한다. 원격 본문의 상대 경로는 이 스킬 폴더 기준이다.
- 도구가 없거나 인증, 네트워크, 서버 호환 문제로 조회할 수 없으면 [동봉된 지침](references/bundled-workflow.md)을 읽는다. 최신 지침을 확인하지 못했음을 알리고 기존 권한과 검증 절차를 지킨다. 지침 조회만을 위해 로그인이나 설치 변경을 강제하지 않는다.
- 동봉된 지침의 상대 경로도 이 스킬 폴더 기준으로 해석한다.
